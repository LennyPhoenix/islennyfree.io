run = "target/release/islennyfree -i 0.0.0.0 -p 80"

[packager]
language = "rust"

[packager.features]
packageSearch = true

[nix]
channel = "unstable"

[gitHubImport]
requiredFiles = [".replit", "replit.nix"]

[languages]

[languages.rust]
pattern = "**/*.rs"
